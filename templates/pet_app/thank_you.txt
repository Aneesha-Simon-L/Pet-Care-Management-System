{% extends 'base.html' %}
{% block section %}
<main class="min-h-screen flex items-center justify-center bg-gradient-to-br from-green-100 via-white to-gray-200 dark:from-gray-800 dark:to-gray-600 py-12 px-4">
  <div class="w-full max-w-3xl bg-white dark:bg-gray-800 rounded-2xl shadow-2xl overflow-hidden">
    <!-- <script src="https://cdn.tailwindcss.com"></script> -->
    
    <!-- Header Section -->
    <div class="bg-green-600 text-white text-center py-6 px-4">
      <h1 class="text-4xl font-extrabold">Thank You for Registering🧡</h1>
      <p class="text-lg mt-2 text-white">Here are the details of your registered pet:</p>
    </div>
    <!-- Pet Detail Card -->
    <div class="bg-gray-100 dark:bg-gray-700 p-6 text-left">
      <p><strong class="text-black dark:text-white">Name:</strong> {{ pet.name }}</p>
      <p><strong class="text-black dark:text-white">Category:</strong> {{ pet.category }}</p>
      <p><strong class="text-black dark:text-white">Breed:</strong> {{ pet.breed }}</p>
      <p><strong class="text-black dark:text-white">Age:</strong> {{ pet.age }}</p>
      <p><strong class="text-black dark:text-white">Gender:</strong> {{ pet.gender }}</p>
      <p><strong class="text-black dark:text-white">Status:</strong> {{ pet.status }}</p>

      <!-- Pay Now Button -->

      <button id="pay-btn" 
        class="inline-block bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg shadow-lg transition">
        Pay Now
      </button>

    <!-- <div class="mt-4 lg:mt-0 lg:ml-6">
      <a href="" 
         class="inline-block bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg shadow-lg transition">
        Pay Now
      </a>
    </div> -->
    
      
      {% if pet.photo %}
      <img src="{{ pet.photo.url }}" alt="Pet Photo"
      class="mt-6 max-h-96 w-auto object-contain rounded mx-auto"> 
      {% endif %}
    </div>
  </div>
</main>
{% endblock %}


<!-- Pet Detail Card -->
<!-- <div class="bg-gray-100 dark:bg-gray-700 p-6 text-left">
  <div class="flex flex-col lg:flex-row lg:items-start lg:justify-between"> -->
    <!-- Pet Details -->
    <!-- <div class="space-y-2 lg:w-2/3">
      <p><strong class="text-black dark:text-white">Name:</strong> {{ pet.name }}</p>
      <p><strong class="text-black dark:text-white">Category:</strong> {{ pet.category }}</p>
      <p><strong class="text-black dark:text-white">Breed:</strong> {{ pet.breed }}</p>
      <p><strong class="text-black dark:text-white">Age:</strong> {{ pet.age }}</p>
      <p><strong class="text-black dark:text-white">Gender:</strong> {{ pet.gender }}</p>
      <p><strong class="text-black dark:text-white">Status:</strong> {{ pet.status }}</p>
    </div> -->

    <!-- Pay Now Button -->
    <!-- <div class="mt-4 lg:mt-0 lg:ml-6">
      <a href="{% url 'payment' pet.id %}" 
         class="inline-block bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg shadow-lg transition">
        Pay Now
      </a>
    </div>
  </div> -->

  <!-- {% if pet.photo %}
  <img src="{{ pet.photo.url }}" alt="Pet Photo"
       class="mt-6 max-h-96 w-auto object-contain rounded mx-auto"> 
  {% endif %}
</div> -->
